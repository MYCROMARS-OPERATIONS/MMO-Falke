#include <stdio.h>  
#include <stdlib.h>
#include "../header/mainMenu.h" 

// EXIT MENU
void mainMenu_0(int *m)
{
    //printf("Menu=%d\n", menu);
    printf("%%%%%%%%%%%%%% Program END! %%%%%%%%%%%%%%\n");
    printf("*****************************");
    // exit(1);
    // exit(EXIT_FAILURE);
    exit(EXIT_SUCCESS);
    // return EXIT_FAILURE;
    // return EXIT_SUCCESS;
}