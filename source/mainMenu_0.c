#include <stdio.h>  
#include <stdlib.h>
#include "../header/mainMenu.h" 

// EXIT MENU
void mainMenu_0(int *m)
{
    //printf("Menu=%d\n", menu);
    printf("%%%%%%%%%%%%%% Program END! %%%%%%%%%%%%%%\n");
    printf("*****************************");
    exit(EXIT_SUCCESS);
    // exit(1);
    // exit(EXIT_FAILURE);
    // return EXIT_FAILURE;
    // return EXIT_SUCCESS;
}